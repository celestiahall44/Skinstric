import './UpdateText.css';

function UpdateText() {
	return (
		<div className="update-text" aria-label="Update instruction heading">
			<div className="update-text-label">If A.I. estimate is wrong, select the correct on one.</div>
		</div>
	);
}

export default UpdateText;
